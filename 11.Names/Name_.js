"use strict";
// Define an array called names and store the names of your friends.
const names = ['Ayesha', 'Habiba', 'Anaya', 'Sana'];
// Print each person's name by accessing each element in the list, one at a time.
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}
